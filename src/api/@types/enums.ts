export enum GlobalSearchType {
  USER = "users",
  GROW_POST = "grow_posts",
}

export enum TimelineType {
  SOCIAL = "social",
  WEEDZ = "weedz",
  GROW = "grow",
}

export enum PostType {
  GROW_POST = "grow",
  SOCIAL_POST = "social",
  WEEDZ_POST = "reel",
}
